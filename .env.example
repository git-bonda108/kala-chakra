# Optional. Enables live OpenAI specialist agents and image (vision) OCR of
# uploaded documents. Without it, a grounded local narrator runs and images
# are recorded as unparsed (never guessed).
#
# Put REAL keys in a local .env file (git-ignored) or in Cursor Cloud secrets —
# NEVER in this tracked example file.
OPENAI_API_KEY=
# Optional migration helper (do not commit a real value):
GITHUB_TOKEN=
